@page
@model OdeToFood.Pages.Restaurants.ListModel
@{
    
    ViewData["Title"] = "List";
}

<h1>Restaurants</h1>
@if (Model.Message != null)
{
    <div class="alert alert-info">@Model.Message</div>
}
<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control"
                   asp-for="SearchTerm" />
            <span class="input-group-append">
                <button class="btn btn-light border">
                    <i class="fa fa-search"></i>
                </button>
            </span>
        </div>
    </div>
</form>


@foreach(var restaurant in Model.Restaurants)
{
    <partial name="_Summary" model="restaurant" />
}

@*<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Location</th>
            <th>Cuisine</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var restaurant in Model.Restaurants)
        {
            <tr>
                <td>@restaurant.Name</td>
                <td>@restaurant.Location</td>
                <td>@restaurant.Cuisine</td>
                <td>
                    <a asp-page="./Detail" asp-route-Id="@restaurant.Id" class="btn btn-info btn-sm">Detail</a>
                    <a asp-page="./Edit" asp-route-restaurantId="@restaurant.Id" class="btn btn-info btn-sm">Edit</a>
                    <a asp-page="./Delete" asp-route-restaurantId="@restaurant.Id" class="btn btn-danger btn-sm">Remove</a>

                </td>
            </tr>
        }
    </tbody>
</table>*@


<a class="btn btn-info mb-2" asp-page="./Edit">Add</a>
<div class="dropdown-divider"></div>
<h6>@Model.MessageConfig</h6>